<Page
    x:Class="TimeLapseCam.Views.ReviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeLapseCam.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:TimeLapseCam.ViewModels"
    xmlns:services="using:TimeLapseCam.Services"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Recordings List -->
        <Grid Grid.Column="0" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Recordings" Style="{StaticResource SubtitleTextBlockStyle}" Margin="10"/>
            
            <!-- Import + Refresh buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="4" Margin="10,0,10,8">
                <Button Content="📂 Import" 
                        Command="{x:Bind ViewModel.ImportVideoCommand}"
                        ToolTipService.ToolTip="Import external video files"
                        FontSize="12" Padding="8,4"/>
                <Button Content="🔄" 
                        Command="{x:Bind ViewModel.RefreshCommand}"
                        ToolTipService.ToolTip="Refresh recordings list"
                        FontSize="12" Padding="8,4"/>
            </StackPanel>
            
            <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Recordings, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedRecording, Mode=TwoWay}"
                      Padding="10" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:RecordingFile">
                        <Grid Margin="0,4" ColumnSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" 
                                      VerticalAlignment="Center" MinWidth="0" Padding="0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{x:Bind CreationTime}" FontSize="11" Foreground="Gray"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Player -->
        <MediaPlayerElement x:Name="Player" Grid.Column="1" 
                            Source="{x:Bind ViewModel.PlaybackSource, Mode=OneWay}"
                            AutoPlay="True" AreTransportControlsEnabled="True"/>

        <!-- Right Panel: Events + Transcription -->
        <Grid Grid.Column="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Object Detection Section -->
            <StackPanel Margin="10" Spacing="6">
                <TextBlock Text="Detection" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <Button Content="🔍 Detect Objects" 
                        Command="{x:Bind ViewModel.AnalyzeCommand}"
                        HorizontalAlignment="Stretch"/>
                <TextBlock Text="{x:Bind ViewModel.AnalysisStatus, Mode=OneWay}" 
                           FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>
            
            <!-- Events List -->
            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Events, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedEvent, Mode=TwoWay}"
                      Padding="10">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="services:EventItem">
                        <StackPanel Margin="0,3">
                            <TextBlock Text="{x:Bind Type}" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap" FontSize="12"/>
                            <TextBlock Text="{x:Bind Timestamp}" Foreground="Gray" FontSize="10"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Transcription Section -->
            <StackPanel Grid.Row="2" Margin="10" Spacing="6">
                <TextBlock Text="Transcription" Style="{StaticResource SubtitleTextBlockStyle}"/>
                
                <!-- Whisper Model Selector -->
                <Grid ColumnSpacing="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Model:" VerticalAlignment="Center" FontSize="12"/>
                    <ComboBox Grid.Column="1" 
                              ItemsSource="{x:Bind ViewModel.WhisperModels, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedWhisperModel, Mode=TwoWay}"
                              HorizontalAlignment="Stretch" FontSize="12"/>
                    <Button Grid.Column="2" Content="⬇"
                            Command="{x:Bind ViewModel.DownloadModelCommand}"
                            ToolTipService.ToolTip="Download or re-download this model"
                            FontSize="12" Padding="8,4"/>
                </Grid>
                
                <Button Content="🎙️ Transcribe Selected" 
                        Command="{x:Bind ViewModel.TranscribeSelectedCommand}"
                        HorizontalAlignment="Stretch"
                        ToolTipService.ToolTip="Transcribe checked recordings (or current if none checked)"/>
                
                <ProgressBar Value="{x:Bind ViewModel.TranscriptionProgress, Mode=OneWay}"
                             Maximum="100" Minimum="0"
                             Visibility="{x:Bind ViewModel.IsTranscribing, Mode=OneWay}"
                             HorizontalAlignment="Stretch"/>

                <TextBlock Text="{x:Bind ViewModel.TranscriptionStatus, Mode=OneWay}" 
                           FontSize="11" Foreground="Gray" TextWrapping="Wrap"/>

                <!-- Transcript version selector -->
                <ComboBox ItemsSource="{x:Bind ViewModel.Transcripts, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.SelectedTranscript, Mode=TwoWay}"
                          DisplayMemberPath="ModelName"
                          HorizontalAlignment="Stretch" FontSize="12"
                          PlaceholderText="No transcripts yet"/>
            </StackPanel>
            
            <!-- Transcript Text -->
            <ScrollViewer Grid.Row="3" Padding="10">
                <TextBlock Text="{x:Bind ViewModel.TranscriptText, Mode=OneWay}" 
                           TextWrapping="Wrap" FontSize="12" IsTextSelectionEnabled="True"/>
            </ScrollViewer>

            <!-- Debug Log (toggle via Settings) -->
            <ScrollViewer Grid.Row="4" Padding="6" MaxHeight="150"
                          Visibility="{x:Bind ViewModel.ShowDebugLog, Mode=OneWay}"
                          Background="#1E1E1E" CornerRadius="4" Margin="6">
                <TextBlock Text="{x:Bind ViewModel.TranscriptionDebugLog, Mode=OneWay}"
                           FontFamily="Consolas" FontSize="10" Foreground="#CCCCCC"
                           TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
