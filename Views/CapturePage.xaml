<Page
    x:Class="TimeLapseCam.Views.CapturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeLapseCam.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:TimeLapseCam.ViewModels"
    xmlns:services="using:TimeLapseCam.Services"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Camera Preview & Selector -->
        <Grid Grid.Column="0" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ComboBox ItemsSource="{x:Bind ViewModel.Cameras, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedCamera, Mode=TwoWay}"
                      DisplayMemberPath="Name"
                      HorizontalAlignment="Stretch" Margin="10"/>

            <Grid Grid.Row="1" Margin="10" Background="Black" CornerRadius="4" BorderBrush="Gray" BorderThickness="1">
                <Image Source="{x:Bind ViewModel.PreviewSource, Mode=OneWay}" Stretch="Uniform" />
                
                <!-- Overlay Info -->
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10">
                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Foreground="Lime" FontSize="14"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Side Panel: Event Log -->
        <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" 
              Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
              BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Event Log" Style="{StaticResource SubtitleTextBlockStyle}" Margin="10"/>
            
            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.RecentEvents, Mode=OneWay}"
                      Padding="10">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="services:EventItem">
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{x:Bind Type}" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="{x:Bind Description}" TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind Timestamp}" Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Bottom Controls -->
        <StackPanel Grid.Column="0" Grid.Row="1" 
                    Orientation="Vertical" HorizontalAlignment="Stretch" 
                    Padding="20" Spacing="8" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20">
                <Button Command="{x:Bind ViewModel.ToggleRecordingCommand}" 
                        Content="{x:Bind ViewModel.RecordButtonText, Mode=OneWay}"
                        Style="{StaticResource AccentButtonStyle}"
                        Height="40" Width="120"/>

                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" 
                           VerticalAlignment="Center"/>
            </StackPanel>
            
            <TextBlock Text="{x:Bind ViewModel.GoogleDriveStatus, Mode=OneWay}" 
                       FontSize="11" Foreground="Gray" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
